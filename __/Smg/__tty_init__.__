class Smg
  var public MSGROW    = LINES - 1;
  var public PROMPTROW = MSGROW - 1;

  let COLOR = NULL;

  def __HLINE__ ()
    repeat (char (8212), COLUMNS);
  end

  def at_exit ()
  end

  def is_inited ()
    0;
  end

  def send_msg_dr (msg)
    IO.tostdout (msg);
  end

  def send_msg (msg)
    IO.tostdout (msg);
  end
end
