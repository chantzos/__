subclass Prev
  __init__
    set
    get
  end

  def invalid_key () proc
    NULL;
  end

  var PREV_ACT = Assoc_Type[Ref_Type, &invalid_key];

  def set (key, ref)
    PREV_ACT[key] = ref;
  end

  def get (key)
    PREV_ACT[key];
  end
end
